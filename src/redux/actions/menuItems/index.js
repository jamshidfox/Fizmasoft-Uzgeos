// ** HANDLE LAYER OPEN

export const handleLayerOpen = (menu) => (dispatch) => dispatch({ type: "HANDLE_LAYER_MENU", menu })

// ** HANDLE DATABASE OPEN
export const handleDatabaseOpen = (menu) => (dispatch) => dispatch({ type: "HANDLE_DATABASE_MENU", menu })

// ** HANDLE INTEGRATION OPEN
export const handleIntegrationOpen = (menu) => (dispatch) => dispatch({ type: "HANDLE_INTEGRATION_MENU", menu })
